# Database Configuration Properties
# Login Demo Application

# SQL Server Configuration (Default)
sqlserver.url=jdbc:sqlserver://localhost:1433;databaseName=BaiTap2;encrypt=true;trustServerCertificate=true
sqlserver.username=sa
sqlserver.password=Admin@123
sqlserver.driver=com.microsoft.sqlserver.jdbc.SQLServerDriver


# Active Database Configuration
# Change this to switch between databases: sqlserver or mysql
active.database=sqlserver

# Connection Pool Settings
connection.pool.initial.size=5
connection.pool.max.size=20
connection.pool.timeout=30000

# Application Settings
app.name=Login Demo Application
app.version=1.0.0
app.session.timeout=30
